services:
  db:
    image: postgres:16-alpine
    container_name: zikir-hatmi-db
    environment:
      POSTGRES_DB: zikirhatmi
      POSTGRES_USER: zikirhatmi
      POSTGRES_PASSWORD: zikirhatmi
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB -h localhost"]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - zikirhatmi_db:/var/lib/postgresql/data

  backend:
    build:
      context: ./zikir-hatmi-backend
    container_name: zikir-hatmi-backend
    environment:
      PORT: "8080"
      DATABASE_URL: "postgres://zikirhatmi:zikirhatmi@db:5432/zikirhatmi?sslmode=disable"
      # Security configuration (set these for production):
      # ALLOWED_ORIGINS: "yourdomain.com"  # Comma-separated list of allowed WebSocket origins
      # ADMIN_KEY: ""                       # Admin key for /hatims listing endpoint
      # TRUST_PROXY: "true"                 # Set to "true" when behind a reverse proxy
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8998:8080"

  frontend:
    build:
      context: ./zikir-hatmi-frontend
    container_name: zikir-hatmi-frontend
    environment:
      # Local dev: proxy API + WebSocket to the backend service inside the compose network.
      VITE_PROXY_TARGET: "http://backend:8080"
    depends_on:
      - backend
    ports:
      - "8999:5173"

volumes:
  zikirhatmi_db:
